import { RatingFormatPipe } from './rating-format.pipe';

describe('RatingFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
